function CreateEventPage() {
  return (
    <section className="dashboard-content-grid">
      <article className="dashboard-card dashboard-card-hero">
        <p className="dashboard-card-kicker">Create event</p>
        <h2>Event builder comes next.</h2>
        <p>
          The auth flow is now the gatekeeper. After this, we can add the form and persist organizer events.
        </p>
      </article>
    </section>
  );
}

export default CreateEventPage;
