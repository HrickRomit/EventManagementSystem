function MyRegistrationsPage() {
  return (
    <section className="dashboard-content-grid">
      <article className="dashboard-card dashboard-card-hero">
        <p className="dashboard-card-kicker">My registrations</p>
        <h2>No event registrations yet.</h2>
        <p>
          Once we build the participant registration flow, booked events and ticket status will appear here.
        </p>
      </article>
    </section>
  );
}

export default MyRegistrationsPage;
